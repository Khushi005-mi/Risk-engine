
import json

    
class FeatureList:

    @staticmethod
    def save(features, path):
        with open(path, "w") as f:
            json.dump(features, f, indent=4)

    @staticmethod
    def load(path):
        with open(path, "r") as f:
            return json.load(f)
