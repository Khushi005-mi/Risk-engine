# it tells the env:
> what liabiries must exist
> the exact versions req
> optional constraints
# with this the prj can reproduce reliability on another machine
# project.toml = blueprint 
# requirements.txt = install list
pandas = 2.2.2
numpy = 1.26.4
scikit-learn == 1.4.2
scipy == 1.13.1
pyyaml == 6.0.1
joblib == 1.4.2
